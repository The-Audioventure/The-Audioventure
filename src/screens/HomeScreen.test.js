import {describe, expect, test} from '@jest/globals'

describe('test tests', () => {
    test('test that tests work 1', () => {
        expect(3,).toBe(3);
      });

    test('test that tests work 2', () => {
    expect(4,).toBe(4);
    });
})


